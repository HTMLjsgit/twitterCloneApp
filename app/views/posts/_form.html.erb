<%= form_with model: @post, local: true do |f| %>
	<%= f.text_area :body %><br>
	<%= f.text_field :tag_list, value: @post.tag_list.join(',') %><br>
	<%= f.file_field :image %><br>
	<%= f.file_field :video %><br>
	<%= f.submit %>
<% end %>
